For using classes: "Redis" and "RedisException" you have to install PHP-REDIS.
Instal commands for UBUNTU:
 Use the package manager.
	sudo apt-get install php-redis

if your php version is not from the standard repository
	sudo apt-get install php7.2-redis 
where "7.2" needing version